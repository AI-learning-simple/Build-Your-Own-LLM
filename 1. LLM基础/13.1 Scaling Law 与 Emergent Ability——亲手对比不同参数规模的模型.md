# 13.1 Scaling Law 与 Emergent Ability——亲手对比不同参数规模的模型

> **本节目标**：Scaling Law 说"模型越大，能力越强"；Emergent Ability 说"某些能力会在规模跨过某个门槛后突然出现"。这两个结论听起来很抽象，本节我们用同一个任务，同时喂给三个不同参数规模的模型（0.5B / 1.5B / 3B），亲手观察这条"越来越强"的曲线到底是什么样子。
>
> **硬件要求**：三个模型都用 4-bit 量化加载（13.4 节会详细讲量化原理），**总显存占用约 3~4GB，8GB 显卡完全够用，甚至可以三个模型都常驻显存同时对比**。

---

# 一、环境准备

```bash
pip install torch transformers accelerate bitsandbytes
```

`bitsandbytes` 是本节用到的量化库，让我们能用更小的显存加载模型（4-bit 量化会在 13.4 节详细展开，这里先直接使用）。

---

# 二、批量加载三个不同规模的模型

```python
import torch
from transformers import AutoTokenizer, AutoModelForCausalLM, BitsAndBytesConfig

quant_config = BitsAndBytesConfig(load_in_4bit=True, bnb_4bit_compute_dtype=torch.float16)

model_names = [
    "Qwen/Qwen2.5-0.5B-Instruct",
    "Qwen/Qwen2.5-1.5B-Instruct",
    "Qwen/Qwen2.5-3B-Instruct",
]

models = {}
tokenizers = {}
for name in model_names:
    tokenizers[name] = AutoTokenizer.from_pretrained(name)
    models[name] = AutoModelForCausalLM.from_pretrained(name, quantization_config=quant_config, device_map="auto")

print("三个模型全部加载完成")
```

---

# 三、设计一个能体现规模差异的任务

Scaling Law 的效果在简单任务上不容易看出来（0.5B 模型也能回答"中国的首都是哪里"），需要选一个**稍微复杂、需要多步推理**的问题，才能看出规模的差异：

```python
prompt = "小明有15个苹果，他把其中的3/5送给了小红，又买了7个，请问小明现在有多少个苹果？请说明计算过程。"

def ask(model_name, prompt):
    tok = tokenizers[model_name]
    model = models[model_name]
    messages = [{"role": "user", "content": prompt}]
    text = tok.apply_chat_template(messages, tokenize=False, add_generation_prompt=True)
    inputs = tok(text, return_tensors="pt").to(model.device)
    output = model.generate(**inputs, max_new_tokens=200, do_sample=False)
    return tok.decode(output[0][inputs["input_ids"].shape[1]:], skip_special_tokens=True)

for name in model_names:
    print(f"\n========== {name} ==========")
    print(ask(name, prompt))
```

---

# 四、观察结果：Scaling Law 的直观体现

正确答案是：`15 × 3/5 = 9`，送出 9 个剩 `15 - 9 = 6`，再买 7 个，`6 + 7 = 13`。运行上面的代码，你通常会观察到：

- **0.5B**：可能算错分数（例如把 3/5 算成 0.5），或者步骤混乱、跳步；
- **1.5B**：大概率能算对分数，但偶尔在多步累加时出错；
- **3B**：通常能完整、正确地写出每一步，并得到正确答案。

这正是 Scaling Law 最直接的体现：**同一个模型家族、同样的训练方式，仅仅是参数规模不同，多步推理的稳定性就有明显差异。**

---

# 五、体验 Emergent Ability：换一个更难的任务

再换一个需要"组合多种能力"的任务，例如简单的代码 + 逻辑推理：

```python
prompt2 = "写一个 Python 函数，判断一个字符串是否是回文串，并解释为什么你的实现是正确的。"

for name in model_names:
    print(f"\n========== {name} ==========")
    print(ask(name, prompt2))
```

你可能会观察到一个更明显的"跃迁"现象：0.5B 模型可能只能写出**能运行但逻辑有瑕疵**的代码，甚至漏掉"解释原因"这部分要求；而 3B 模型不仅代码正确，还能按要求给出清晰解释——这种"不是慢慢变好，而是某个规模突然就能完整完成任务"的现象，正是第13章讲的 **Emergent Ability**。

---

# 六、给这个观察打个分（可选，量化对比）

如果想更严谨地对比，可以设计 5~10 道类似难度的题目，人工打分（0~1），画出"参数规模 vs 平均得分"的曲线：

```python
import matplotlib.pyplot as plt

# 示例分数，实际使用时请替换成你自己测试打出的真实分数
scores = {"0.5B": 0.3, "1.5B": 0.6, "3B": 0.85}

plt.plot(list(scores.keys()), list(scores.values()), marker="o")
plt.xlabel("模型规模")
plt.ylabel("多步推理任务平均得分")
plt.title("Scaling Law 实测曲线")
plt.show()
```

如果分数是平滑上升，说明这批任务体现的是**渐进的 Scaling Law**；如果某一个规模的分数突然大幅跳升，说明触发了**Emergent Ability**——具体是哪一种，取决于你选的任务难度和模型规模的间隔。

---

# 七、显存占用参考

| 模型 | 4-bit 量化后显存占用 |
|---|---|
| Qwen2.5-0.5B-Instruct | ~0.5GB |
| Qwen2.5-1.5B-Instruct | ~1.2GB |
| Qwen2.5-3B-Instruct | ~2.2GB |
| 三个模型合计 | ~4GB（8GB 显存留有充足余量） |

---

# 本节小结

✅ 同时加载并对比了 0.5B / 1.5B / 3B 三个不同规模的真实模型 ✅ 用多步推理任务亲眼观察到"规模越大，稳定性越强"的 Scaling Law ✅ 用组合型任务观察到"某个规模后能力突然完整出现"的 Emergent Ability ✅ 三个模型 4-bit 量化后总显存不到 4GB，8GB 显卡轻松胜任

下一节，我们将体验大模型另一个神奇的能力——不用训练、只靠 Prompt 就能"学会"新任务：**13.2 In-Context Learning——亲手体验 Few-shot / Zero-shot**。
