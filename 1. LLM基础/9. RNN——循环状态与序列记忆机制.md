# 9. RNN——循环状态与序列记忆机制

> **本章目标**
>
> 第6章构造了固定窗口 Neural Language Model，第7、8章完成了训练方法。这个模型每次只能接收固定数量的 Context Token，窗口外的信息不会进入当前预测。
>
> 本章将学习固定 Context Window 的结构限制、Long-Term Dependency、RNN 的循环状态、Hidden State，以及 LSTM 如何通过门控机制改善长期信息传播。RNN 的关键变化不是简单扩大窗口，而是在处理序列时把前一时间步的状态传给下一时间步。

---

# 固定窗口的问题

第5章提出过固定 Context Window 看不到远处信息的问题，第6～8章的模型也一直受这个限制。本章先用一段可运行代码把它验证清楚，再给出解决思路。

以窗口大小为 2、句子 `I love AI today` 为例，模型只能看到 `[I love] → AI`、`[love AI] → today`——始终只有最近两个 Token，看不到整个句子。

再看一个更长的句子：`The book that I bought yesterday from my best friend is interesting.`。最后的 `interesting` 真正描述的是 `book`，但如果窗口只有 4，模型看到的 Context 是 `my best friend is`，`book` 早已消失，模型根本不知道 `interesting` 修饰的是什么。

这种"真正相关的信息距离很远"的现象，在 NLP 中叫做：

> **Long-Term Dependency（长距离依赖）**

中文同样存在这个问题：`昨天在北京参加人工智能大会并发表主题演讲的张教授今天回到了上海。`——"张教授"与"回到了上海"之间隔了很多 Token，固定窗口依然无法覆盖。

---

# Experiment 9.0：观察 Context Window 的局限

```python
sentence = ["The","book","that","I","bought","yesterday","is","interesting"]
window = 3

for i in range(window, len(sentence)):
    context = sentence[i-window:i]
    target = sentence[i]
    print(context, "->", target)
```

输出：

```text
['The', 'book', 'that'] -> I
['book', 'that', 'I'] -> bought
['that', 'I', 'bought'] -> yesterday
['I', 'bought', 'yesterday'] -> is
['bought', 'yesterday', 'is'] -> interesting
```

最后一次预测，Context 是 `bought yesterday is`，真正重要的信息 `book` 已经消失，模型永远不知道 `interesting` 描述的是什么。

**思考题**：如果窗口改成 4 或 100，是否能彻底解决问题？窗口不断增大，又会付出什么代价？

---

# 从固定窗口到循环状态

本章先不急于回答"RNN 怎么实现"，而是留下一个问题：

> **能否在逐个读取 Token 时，把历史压缩进一个持续更新的状态？**

下一节将引出 **Memory（记忆）** 与 **Hidden State（隐藏状态）**。这个状态能够跨时间步传递信息，但容量有限，也不能保证永久保留任意久远的内容；这些限制将在后续小节中继续分析。
