# 7.1 使用开源 VLM 做一次零样本抓取点预测，并转换为机械臂关节角度

> **本节目标**：把前面几章学过的东西串成一条完整的流水线——用一个开源视觉语言模型（VLM），在没有专门训练过抓取任务的情况下，直接"零样本"地在图像上指出应该抓取哪个像素点；再用第5章的相机反投影公式，把这个像素点转换成三维空间坐标；最后用第2章的逆运动学，算出机械臂应该把关节转到多少度。

> **关于本节代码的运行说明**：第一部分调用的开源 VLM 需要下载数GB的模型权重、且推理通常需要 GPU 才能在合理时间内完成，本节不在当前写作环境里实际下载运行，只提供真实可用、可以直接复制到你自己机器上运行的代码；第二、三部分（反投影、逆运动学）复用的是第5、2章已经验证过的纯数学公式，代码严格可运行，并给出手工推导验证过的精确结果。

---

# 一、第一步：用开源 VLM 做零样本抓取点预测

我们使用 Hugging Face 上的开源视觉语言模型（以 `Qwen2-VL-2B-Instruct` 为例，这是一个体积较小、消费级显卡也能跑起来的开源 VLM），直接用自然语言"指挥"它在图像上标出一个像素坐标：

```bash
pip install transformers accelerate pillow torch
```

```python
from transformers import Qwen2VLForConditionalGeneration, AutoProcessor
from PIL import Image

model_name = "Qwen/Qwen2-VL-2B-Instruct"
model = Qwen2VLForConditionalGeneration.from_pretrained(model_name, torch_dtype="auto", device_map="auto")
processor = AutoProcessor.from_pretrained(model_name)

image = Image.open("mug_on_table.jpg")   # 换成你自己的场景照片，图像分辨率假设为 640x480

prompt = (
    "这是一张桌面场景的照片，图像大小为640x480像素，左上角是像素坐标原点(0,0)。"
    "请找到杯子的把手，只回答一个像素坐标，格式严格为 (u, v)，不要输出任何其他文字。"
)

messages = [{"role": "user", "content": [{"type": "image"}, {"type": "text", "text": prompt}]}]
text = processor.apply_chat_template(messages, tokenize=False, add_generation_prompt=True)
inputs = processor(text=[text], images=[image], return_tensors="pt").to(model.device)

output_ids = model.generate(**inputs, max_new_tokens=20)
response = processor.batch_decode(output_ids[:, inputs.input_ids.shape[1]:], skip_special_tokens=True)[0]
print("VLM 输出:", response)   # 期望格式类似 "(360, 250)"
```

这种"直接让 VLM 用文字描述一个像素坐标"的用法，被称为 **Visual Prompting / Pointing**，是当前用现成 VLM 做零样本机器人感知（不需要专门训练检测器或抓取网络）的一种真实、常见的技术路线（一些工作如 RoboPoint、MOKA 采用了类似思路）。VLM 之所以能做到这一点，正是因为它在海量图文数据上预训练时，已经具备了理解"物体在图像中哪个区域"这种视觉定位的语义能力——这与第7章讨论的 RT-2"语义泛化能力来自互联网预训练"是同一个原理的另一种体现。

假设 VLM 返回了 `(360, 250)`，即预测杯子把手中心大致在像素坐标 `u=360, v=250`。

---

# 二、第二步：把 2D 像素坐标反投影成 3D 坐标

这一步严格复用第5章推导并验证过的反投影公式，是纯数学运算，完全确定，可以直接运行：

```python
# 相机内参（与5.1节实验保持一致）
fx, fy = 500.0, 500.0
cx, cy = 320.0, 240.0

u, v = 360, 250                # VLM 给出的抓取点像素坐标
depth = 0.8                    # 假设 RGB-D 相机在这个像素处读到的深度值是 0.8 米

X = (u - cx) * depth / fx
Y = (v - cy) * depth / fy
Z = depth

print(f"抓取点在相机坐标系下的三维坐标: X={X:.3f}, Y={Y:.3f}, Z={Z:.3f}")
```

按照公式手工计算：`X = (360-320)×0.8/500 = 40×0.8/500 = 0.064`，`Y = (250-240)×0.8/500 = 10×0.8/500 = 0.016`，`Z = 0.8`（深度值本身直接就是Z）。

**预期输出**：

```
抓取点在相机坐标系下的三维坐标: X=0.064, Y=0.016, Z=0.800
```

---

# 三、第三步：用逆运动学算出机械臂关节角度

为了能直接复用第2章的二连杆平面机械臂逆运动学公式，这里做一个简化：只考虑机械臂在"水平前伸方向（对应相机的 X 轴）— 深度方向（对应相机的 Z 轴）"这个竖直平面内的运动（真实的6-DOF机械臂需要完整的三维逆运动学求解器来处理 Y 轴方向的偏移，这里为了聚焦流程本身而简化）。假设这条机械臂的两根连杆各长 `0.5` 米（总臂长 `1.0` 米，覆盖 `0.8` 米深度的目标完全没问题）：

```python
import math

def inverse_kinematics(x, y, l1=0.5, l2=0.5, elbow_up=True):
    """与2.1节完全相同的逆运动学实现"""
    d_squared = x**2 + y**2
    d = math.sqrt(d_squared)
    if d > l1 + l2 or d < abs(l1 - l2):
        return None
    cos_theta2 = (d_squared - l1**2 - l2**2) / (2 * l1 * l2)
    cos_theta2 = max(-1.0, min(1.0, cos_theta2))
    sign = 1 if elbow_up else -1
    theta2 = sign * math.acos(cos_theta2)
    theta1 = math.atan2(y, x) - math.atan2(l2 * math.sin(theta2), l1 + l2 * math.cos(theta2))
    return theta1, theta2

def forward_kinematics(theta1, theta2, l1=0.5, l2=0.5):
    x = l1 * math.cos(theta1) + l2 * math.cos(theta1 + theta2)
    y = l1 * math.sin(theta1) + l2 * math.sin(theta1 + theta2)
    return x, y

# 用 (X, Z) 作为机械臂运动平面内的目标坐标: 水平方向X=0.064，深度方向Z=0.8
target_x, target_z = X, Z
result = inverse_kinematics(target_x, target_z, l1=0.5, l2=0.5, elbow_up=True)
theta1, theta2 = result

print(f"机械臂关节角度: theta1={math.degrees(theta1):.2f}°, theta2={math.degrees(theta2):.2f}°")

# 用正运动学验证一下算出来的关节角度对不对
check_x, check_z = forward_kinematics(theta1, theta2, l1=0.5, l2=0.5)
print(f"正运动学验证还原位置: ({check_x:.3f}, {check_z:.3f})  目标位置: ({target_x:.3f}, {target_z:.3f})")
```

按照第2章公式手工推导（`l1=l2=0.5`，目标 `(0.064, 0.8)`）：`d² = 0.064² + 0.8² ≈ 0.6441`，`cos(theta2) = (0.6441-0.25-0.25)/0.5 ≈ 0.288`，`theta2 = arccos(0.288) ≈ 73.24°`；代入求得 `theta1 ≈ 48.81°`。

**预期输出**（手工推导结果，实际运行浮点精度可能在小数点后第2~3位略有出入）：

```
机械臂关节角度: theta1=48.81°, theta2=73.24°
正运动学验证还原位置: (0.064, 0.800)  目标位置: (0.064, 0.800)
```

正运动学验证结果与目标位置一致，说明这条"VLM 指出抓取点 → 反投影成三维坐标 → 逆运动学算出关节角度"的完整流水线，在数学上是自洽、正确的。

---

# 四、这条流水线说明了什么

```mermaid
flowchart LR
    A["开源 VLM<br/>零样本指出2D抓取点"] --> B["第5章：相机反投影<br/>2D像素 → 3D坐标"] --> C["第2章：逆运动学<br/>3D坐标 → 关节角度"] --> D["机械臂执行"]
```

这条流水线是一种**模块化（Modular）的机器人智能实现方式**：每一步都是一个独立、可解释、可单独调试的模块，VLM 只负责"语义理解"（这一步不需要为具体机器人做任何专门训练），后面两步是纯几何计算。这和第7章讲的**端到端 VLA**（一个模型直接从图像+语言输出动作）形成了鲜明对比——**端到端方案潜力更大（能学到模块化方案覆盖不到的隐式策略），但需要大量机器人专属数据训练；模块化方案则可以立刻用现成的开源 VLM"零样本"跑起来，代价是每一步都依赖前一步的准确性（如果 VLM 指错了点，或者深度读数有噪声，误差会一路传递到最终的关节角度）。** 在实际工程中，这两种思路经常结合使用：用 VLA 或专用策略网络负责精细操作，用模块化的 VLM 感知负责快速适配新物体、新场景。

---

# 本实验总结

✅ 完整走通了一条"VLM 零样本感知 → 相机反投影 → 逆运动学"的模块化机器人智能流水线，把第2、5、7三章的知识首尾相连 ✅ 理解了模块化方案与端到端 VLA 方案各自的优势和适用场景 ✅ 亲手验证了流水线中的几何计算部分（反投影、逆运动学）在数学上完全自洽

下一章，我们回到强化学习这条主线，讨论真实世界里的奖励设计问题：**8. 强化学习进阶——奖励设计与真实世界强化学习**。
