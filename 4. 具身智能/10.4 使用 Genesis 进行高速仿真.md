# 10.4 使用 Genesis 进行高速仿真

> **本节目标**：安装新一代仿真器 Genesis，理解它相比 MuJoCo、Isaac Lab 的核心卖点——极致的仿真速度和原生的可微分仿真（Differentiable Simulation）能力，并给出真实可用的参考代码。

> **运行环境说明**：Genesis 是2024年发布、仍在快速迭代中的新项目，既支持 CPU 也支持 GPU 后端（GPU 后端速度更快，但 CPU 后端也可以运行基础功能，硬件门槛比 Isaac Lab 低）。以下代码基于 Genesis 官方文档给出的典型使用模式，具体 API 可能随版本更新调整，建议在实际运行前查阅最新的官方文档（genesis-world.readthedocs.io）确认接口细节。

---

# 一、Genesis 解决的是什么问题

MuJoCo 精确但不是为大规模并行设计的；Isaac Lab 支持大规模并行但依赖英伟达的重型软件栈、安装复杂。**Genesis 的定位是：用一个相对轻量的 Python 包，同时提供"接近或超过 Isaac Lab 的仿真速度"和"原生支持可微分仿真"**——后者是一个前面几章还没有深入讨论过的能力：**如果整个物理仿真过程本身是可微分的（可以对仿真结果求梯度），就可以像训练神经网络一样，直接用梯度下降去优化一条动作序列或一个控制器的参数，而不需要像第 9 章的随机打靶（Random Shooting）那样盲目采样大量候选方案。**

```mermaid
flowchart LR
    A["候选动作序列"] --> B["可微分物理仿真"] --> C["任务目标(如末端到目标的距离)"]
    C -- "直接反向传播梯度" --> A
```

这本质上是第 9 章 MPC 规划思路的一次升级：第 9 章的随机打靶完全依赖"随机采样+挑最好的"，而如果仿真过程可微分，就可以用梯度直接告诉我们"动作序列应该往哪个方向调整，才能让任务目标变得更好"——这和第一部分训练神经网络时"用梯度下降更新参数"是完全相同的数学工具，只是这里被直接用在了物理仿真和动作规划上。

---

# 二、安装与基础用法（参考代码，具体接口请以官方文档为准）

```bash
pip install genesis-world
```

```python
import genesis as gs

gs.init(backend=gs.cpu)   # 没有独立显卡也可以用 CPU 后端运行；有 NVIDIA 显卡可以换成 gs.gpu 获得更快的速度

scene = gs.Scene(show_viewer=False)   # show_viewer=False 表示不打开可视化窗口，适合无显示器环境

# 添加一个地面和一个球体（Genesis 提供了一批基础几何体，也支持加载URDF/MJCF等标准机器人模型文件）
plane = scene.add_entity(gs.morphs.Plane())
ball = scene.add_entity(gs.morphs.Sphere(radius=0.1, pos=(0, 0, 1.0)))

scene.build()

for i in range(100):
    scene.step()
    if i % 20 == 0:
        pos = ball.get_pos()
        print(f"step={i}: 球的位置 = {pos}")
```

**预期趋势**：这是一个最基础的"自由落体"仿真——一个初始高度为1.0米的球，在重力作用下应该逐渐下落，`球的位置`里的高度分量（通常是 z 轴）应该随着 `step` 的增加持续减小，直到落到地面（`z≈0.1`，等于球的半径）附近趋于稳定。这和第 9 章我们手写的"点导航"环境的核心区别在于：**这里的下落速度、加速度、与地面碰撞后的反弹或静止行为，全部由 Genesis 内置的物理引擎根据真实的重力常数、材质属性自动计算，不需要我们手写任何运动方程。**

---

# 三、大规模并行：与 Isaac Lab 同样的能力，更轻量的实现

```python
scene_batch = gs.Scene(show_viewer=False)
plane = scene_batch.add_entity(gs.morphs.Plane())
ball = scene_batch.add_entity(gs.morphs.Sphere(radius=0.1, pos=(0, 0, 1.0)))
scene_batch.build(n_envs=1000)   # 一次性并行构建1000个独立的场景副本

for i in range(50):
    scene_batch.step()

print("并行环境数量:", scene_batch.n_envs)
```

**这与10.3节 Isaac Lab 的 `num_envs=4096` 是同一个思路——用 GPU（或多核 CPU）的并行计算能力，一次性仿真大量独立的环境副本**，只是 Genesis 用一个体积更小、安装更简单的 Python 包实现了类似的能力，这也是它自发布以来迅速获得关注的原因之一。

---

# 四、四款仿真器的最终对比

亲手体验完全部四款仿真器之后，回头再看本部分第 10 章"主流仿真器一览"那张对比表格，现在应该有了更具体的感受：

| 仿真器 | 本节体验到的核心特点 |
|---|---|
| PyBullet | 安装最简单、最轻量，适合快速原型验证和教学 |
| MuJoCo | 物理精度高，是学术界事实标准，Gymnasium 内置环境大量基于它 |
| Isaac Lab | 依赖 NVIDIA GPU，能做到数千环境级别的大规模并行，但安装门槛高 |
| Genesis | 追求极致速度且支持可微分仿真，安装相对轻量，是四者中最新、仍在快速演进的一个 |

---

# 五、思考题

如果仿真过程是可微分的，理论上可以直接对"整条动作序列"做梯度下降优化，得到一个比第 9 章随机打靶更精确的规划结果。可以思考一下：这种"通过物理仿真本身反向传播梯度"的方法，相比第 9 章学习一个显式的动态模型再做 MPC 规划，可能存在哪些局限（提示：可以考虑"真实的接触碰撞过程往往是不连续的（比如物体是否碰到了另一个物体），这种不连续性对于要求处处可微的梯度方法意味着什么"）？

---

# 本实验总结

✅ 安装并运行了 Genesis，理解了它相比其它三款仿真器的核心差异化能力——高速仿真与原生可微分仿真 ✅ 理解了"可微分仿真"如何把第 9 章基于采样的规划方法，升级为可以直接用梯度下降优化的规划方法 ✅ 完整对比了 PyBullet、MuJoCo、Isaac Lab、Genesis 四款仿真器在安装门槛、物理精度、并行规模、特色能力上的差异，为实际项目中的仿真器选型建立了具体的感性认识

至此，第 10 章关于仿真器的四节动手实验全部完成。下一章，我们讨论仿真训练好的策略，如何真正跨越到真实世界：**11. Sim-to-Real——从虚拟世界走向真实世界**。
