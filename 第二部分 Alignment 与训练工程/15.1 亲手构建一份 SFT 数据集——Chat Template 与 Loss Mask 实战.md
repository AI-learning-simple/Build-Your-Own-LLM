# 15.1 亲手构建一份 SFT 数据集——Chat Template 与 Loss Mask 实战

> **本节目标**：第12.3节我们已经用 LoRA 完整跑过一次指令微调的训练过程，但那一节把"数据怎么变成模型能学习的样子"这一步完全隐藏在了 `Dataset` 类里。这一节我们专门把这一步拆开来看：一条 `{指令, 回答}` 的原始数据，究竟是怎么变成 Token、又是怎么变成 Loss Mask 的？为什么模型只学习"回答"，却不会学习"提问"？

---

## 一、原始指令数据

```python
raw_data = [
    {"instruction": "介绍一下 Transformer。", "output": "Transformer 是一种基于 Self-Attention 的神经网络结构，由 Google 于 2017 年提出。"},
    {"instruction": "1+1等于几？", "output": "1+1等于2。"},
]
```

---

## 二、用 Chat Template 拼接成对话格式

```python
from transformers import AutoTokenizer

# 加载模型自带的 Tokenizer，其中已经保存了这个模型专属的 Chat Template
tok = AutoTokenizer.from_pretrained("Qwen/Qwen2.5-0.5B-Instruct")

def build_messages(sample):
    # 把 {instruction, output} 转换成 Chat Template 认识的 role/content 结构
    return [
        {"role": "user", "content": sample["instruction"]},      # 用户提问
        {"role": "assistant", "content": sample["output"]},      # 期望模型给出的回答
    ]

sample = raw_data[0]
messages = build_messages(sample)
# apply_chat_template 会自动拼接模板标记（如 <|im_start|>user ... <|im_end|>）
# tokenize=False 表示先返回拼接好的纯文本，方便肉眼查看格式
full_text = tok.apply_chat_template(messages, tokenize=False)
print(full_text)
```

你会看到类似这样的结构（不同模型的模板标记略有不同）：

```text
<|im_start|>user
介绍一下 Transformer。<|im_end|>
<|im_start|>assistant
Transformer 是一种基于 Self-Attention 的神经网络结构……<|im_end|>
```

---

## 三、为什么需要 Loss Mask？

如果直接把整段文本喂给模型做 Next Token Prediction，模型会**同时学习"如何提问"和"如何回答"**——但我们真正希望模型学会的只是"如何回答"。因此需要构造一个 `labels` 数组：**用户提问部分标记为 `-100`（PyTorch 的 `CrossEntropyLoss` 会自动忽略 `-100` 的位置），只保留 assistant 回答部分的真实 Token ID**。

```python
import torch

def build_training_sample(sample, tokenizer):
    messages = build_messages(sample)

    # 只拼接到"用户提问"为止（add_generation_prompt=True 会加上等待 assistant 开始回答的标记）
    prompt_only = tokenizer.apply_chat_template(messages[:1], tokenize=False, add_generation_prompt=True)
    # full_text 是包含 assistant 完整回答的整段对话
    full_text = tokenizer.apply_chat_template(messages, tokenize=False)

    # 分别把两段文本转成 Token ID，用于确定"回答部分"从哪个位置开始
    prompt_ids = tokenizer(prompt_only, add_special_tokens=False)["input_ids"]
    full_ids = tokenizer(full_text, add_special_tokens=False)["input_ids"]

    # 核心：构造 Loss Mask —— 前 len(prompt_ids) 个位置（提问+模板标记）设为 -100，训练时会被忽略；
    # 从这之后的位置保留真实 Token ID（即 assistant 的回答），这部分才会计算 Loss
    labels = [-100] * len(prompt_ids) + full_ids[len(prompt_ids):]

    assert len(labels) == len(full_ids), "labels 长度必须和 input_ids 一致"

    return {
        "input_ids": torch.tensor(full_ids),   # 模型真正看到的输入序列
        "labels": torch.tensor(labels),        # 训练时用于计算 Loss 的目标序列（含 -100 掩码）
    }

encoded = build_training_sample(raw_data[0], tok)
print("input_ids 长度:", len(encoded["input_ids"]))
print("labels 中被 mask 掉(-100)的数量:", (encoded["labels"] == -100).sum().item())     # 对应"用户提问"部分
print("labels 中参与训练的 token 数量:", (encoded["labels"] != -100).sum().item())       # 对应"assistant 回答"部分
```

运行后你会发现，`-100` 的数量正好对应"用户提问 + 模板标记"的长度，而参与训练的 Token 数量正好对应"assistant 回答"的长度——**这就是 SFT 真正学习的内容边界**。

---

## 四、可视化：哪些 Token 参与了训练？

```python
# 把 Token ID 还原成可读的 Token 文本，逐个对照它是否参与了训练
tokens = tok.convert_ids_to_tokens(encoded["input_ids"])
labels = encoded["labels"].tolist()

for t, l in zip(tokens, labels):
    # labels 中不是 -100 的位置才会真正参与 Loss 计算
    mark = "🟢学习" if l != -100 else "⚪忽略"
    print(f"{t:>15}  {mark}")
```

你会清楚地看到：所有属于 `user` 部分和模板标记的 Token都被标记为"⚪忽略"，只有 `assistant` 真正说出的话被标记为"🟢学习"。

---

## 五、用一个真实的前向传播验证

```python
model = AutoModelForCausalLM.from_pretrained("Qwen/Qwen2.5-0.5B-Instruct")

# unsqueeze(0) 给每个 Tensor 增加一个 batch 维度（模型期望输入形如 [batch_size, seq_len]）
batch = {k: v.unsqueeze(0) for k, v in encoded.items()}
# 把 input_ids 和 labels 一起传给模型，HuggingFace 会自动用 labels 计算 CrossEntropyLoss
# 其中 labels 里的 -100 会被自动忽略，这正是我们上一步手工构造的效果
output = model(**batch)

print("Loss:", output.loss.item())
```

能正常输出一个具体的 Loss 数值，说明我们手工构造的 `input_ids`和 `labels` 完全符合模型训练的要求——这正是 12.3 节 LoRA 训练脚本内部 `Dataset` 类真正做的事情，只是这次我们亲手把它拆开、看清楚了每一步。

---

## 本节小结

✅ 用 Chat Template 把原始指令数据拼接成模型能理解的对话格式 ✅ 亲手构造了 Loss Mask，理解了为什么模型只学习"回答"而不学习"提问" ✅ 通过可视化确认了哪些 Token 真正参与了训练 ✅ 用真实前向传播验证了构造的数据格式完全正确。一句话总结：**SFT 真正的"配方"不在模型结构里，而在数据的组织方式和 Loss Mask 里。**

下一节，我们将进入 Preference Learning，亲手训练一个 Reward Model，理解 RLHF 里"谁来打分"这个关键问题：**16.1 亲手训练一个 Reward Model，并观察它如何为回答打分**。
