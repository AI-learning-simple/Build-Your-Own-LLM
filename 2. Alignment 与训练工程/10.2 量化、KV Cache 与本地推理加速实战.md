# 10.2 量化、KV Cache 与本地推理加速实战

> **硬件要求**：需 GPU——量化与推理加速的对比需要 CUDA 才有意义；云 GPU 方案见前言 P0.3
>
> **本节目标**：10.1 节我们关注的是"服务端如何同时服务很多用户"（vLLM + Continuous Batching）。这一节换一个更贴近个人的场景：**一块 8GB 显存的消费级显卡，到底能不能跑动一个 7B 模型？** 我们会以 7B checkpoint 为例，分别介绍 **Hugging Face 生态**（transformers + bitsandbytes）与 **llama.cpp / Ollama**（GGUF）两条量化推理路径；实际测量权重与运行时占用、KV Cache 开关延迟，以及不同上下文长度的缓存张量大小。是否能在特定 8GB 设备运行由平台、量化格式、上下文和运行时共同决定。
>
> **平台边界**：本节路径 A 的 `bitsandbytes` 4-bit 示例按 **NVIDIA CUDA** 环境编写，且需要兼容的驱动、PyTorch 与 bitsandbytes 版本；它不能原样用于 macOS 的 MPS。Apple Silicon 建议走路径 B 的 llama.cpp / Ollama（Metal），此时占用的是统一内存而不是独立“显存”。7B 4-bit 能否在 8GB 设备运行还取决于量化格式、上下文长度、KV Cache、运行时缓冲区和是否发生 CPU offload，不能只由权重大小保证。

---

# 一、为什么 7B 模型原本装不进 8GB？

`Qwen2.5-7B` 有 76 亿参数。如果用 FP16（每个参数占 2 字节）存储，仅权重就需要 `76亿 × 2字节 ≈ 15GB`，还没算推理时的中间激活值和 KV Cache——**远超 8GB 显存**。这正是第一部分第15章 MoE 一节提到的"参数越多、计算/存储代价越大"问题的另一面：**存储代价同样会随参数线性增长**。

---

# 二、量化的核心思想

量化（Quantization）用更少的比特表示权重或激活。FP16 权重每参数 16 bit，4-bit 权重的理想数据 payload 是其 `1/4`；实际还要存分组 scale、zero point 等元数据。最终权重与运行时占用取决于量化方案和实现，必须连同 KV Cache、临时张量、缓冲区与 offload 一起实测。

代价是什么？精度损失。4-bit 量化会让每个参数的数值变得不那么精确，因此模型输出质量会有轻微下降——但对大多数任务，这个损失通常在可接受范围内，这也是"量化"能在消费级硬件上大规模普及的原因。

---

# 三、路径 A：Hugging Face 生态（bitsandbytes 4-bit 量化）

以下代码先显式检查 CUDA，避免在 CPU 或 macOS/MPS 上误运行：

```bash
pip install torch transformers accelerate bitsandbytes
```

```python
import torch
import time
from transformers import AutoTokenizer, AutoModelForCausalLM, BitsAndBytesConfig

if not torch.cuda.is_available():
    raise RuntimeError("此 bitsandbytes 示例需要 NVIDIA CUDA；macOS 请使用下文 Ollama/llama.cpp 路径。")

model_name = "Qwen/Qwen2.5-7B-Instruct"
quant_config = BitsAndBytesConfig(load_in_4bit=True, bnb_4bit_compute_dtype=torch.float16)

tokenizer = AutoTokenizer.from_pretrained(model_name)
model = AutoModelForCausalLM.from_pretrained(model_name, quantization_config=quant_config, device_map="auto")

print("显存占用 (GB):", torch.cuda.memory_allocated() / 1e9)
```

记录本机加载后的 `memory_allocated()`，再分别测试目标 Prompt 长度与生成长度。该值受 transformers/bitsandbytes 版本、量化配置、设备映射和 offload 影响，不能从“4-bit”推定固定占用或剩余空间。

---

# 四、路径 B：llama.cpp / Ollama（GGUF 量化）

`llama.cpp` 是面向本地推理的 C/C++ 引擎，支持 GGUF 量化、CPU/GPU 分层加载和多种硬件后端。它与 transformers 的速度和占用差异取决于量化格式、硬件、内核和 offload 配置，必须在相同模型与输入条件下实测。较易上手的方式之一是 **Ollama**：

```bash
# 安装 Ollama（以 macOS/Linux 为例，Windows 请到官网下载安装包）
curl -fsSL https://ollama.com/install.sh | sh

# 拉取并运行 4-bit 量化的 Qwen2.5-7B（Ollama 会自动下载 GGUF 格式）
ollama run qwen2.5:7b
```

也可以直接调用 Python API，方便和前面章节的实验放在同一个脚本里对比：

```bash
pip install ollama
```

```python
import ollama
import time

start = time.time()
response = ollama.generate(model="qwen2.5:7b", prompt="用一句话介绍 Transformer。")
elapsed = time.time() - start

print(response["response"])
print(f"耗时: {elapsed:.2f}s")
```

Ollama 模型标签对应的具体 GGUF 量化与文件大小可能随发布版本变化。运行时还要分配 KV Cache、计算缓冲区等，并可能在 CPU、Metal 或 CUDA 间分配；请用本机工具记录实际内存/统一内存/显存，而不要由模型文件大小推断能否稳定运行。

---

# 五、两条路径的对比

| | Hugging Face（bitsandbytes） | llama.cpp / Ollama（GGUF） |
|---|---|---|
| 本节平台 | NVIDIA CUDA | CPU / Metal / CUDA（视构建与配置） |
| 接口 | Python `transformers` | 命令行、HTTP/Python API |
| 占用 | 实测权重、KV Cache、临时张量与 offload | 实测模型映射、KV Cache、计算缓冲区 |
| 速度 | 在相同模型、量化、上下文和输出长度下测量 | 在相同模型、量化、上下文和输出长度下测量 |

---

# 六、体验 KV Cache 的效果

回顾第 15 章：KV Cache 保存历史 Token 的 Key/Value，避免每生成一个新 Token 都重新计算全部历史。用 `transformers` 直接对比开启和关闭 KV Cache 的生成速度：

```python
import time

prompt = "请详细介绍一下深度学习的发展历史。"
input_device = model.get_input_embeddings().weight.device
inputs = tokenizer(prompt, return_tensors="pt").to(input_device)


def timed_generate(use_cache):
    # CUDA kernel 异步执行，计时边界必须同步。
    torch.cuda.synchronize()
    start = time.perf_counter()
    with torch.inference_mode():
        output = model.generate(
            **inputs, max_new_tokens=100, use_cache=use_cache, do_sample=False
        )
    torch.cuda.synchronize()
    return output, time.perf_counter() - start


# 两种模式分别短预热，减少首次编译、内存分配等一次性开销。
with torch.inference_mode():
    for cache_mode in (True, False):
        model.generate(
            **inputs, max_new_tokens=8, use_cache=cache_mode, do_sample=False
        )

output_with_cache, time_with_cache = timed_generate(True)
output_no_cache, time_no_cache = timed_generate(False)

print(f"开启 KV Cache 耗时: {time_with_cache:.2f}s")
print(f"关闭 KV Cache 耗时: {time_no_cache:.2f}s")
print(f"速度比: {time_no_cache / time_with_cache:.2f}x")
```

关闭 KV Cache 后，每一步都会重新计算历史 Token 的 K/V；开启后则复用缓存。加速比受 Prompt/输出长度、模型、Attention 实现和硬件影响，应报告本机实测值，不能预设为“数倍”。

---

# 七、体验 Long Context 的显存代价

KV Cache 不是免费的：上下文越长，缓存占用的显存越多。测试不同长度的 Prompt，观察显存变化：

```python
def tensor_bytes(obj):
    if torch.is_tensor(obj):
        return obj.numel() * obj.element_size()
    if isinstance(obj, (tuple, list)):
        return sum(tensor_bytes(item) for item in obj)
    if isinstance(obj, dict):
        return sum(tensor_bytes(item) for item in obj.values())
    if hasattr(obj, "key_cache") and hasattr(obj, "value_cache"):
        return tensor_bytes(obj.key_cache) + tensor_bytes(obj.value_cache)
    return 0


def measure_kv_cache(token_length):
    vocab_size = model.get_input_embeddings().num_embeddings
    dummy_input = torch.randint(
        0, vocab_size, (1, token_length), device=input_device
    )
    with torch.inference_mode():
        outputs = model(dummy_input, use_cache=True)
    cache = outputs.past_key_values
    # 新版 transformers 可能返回 DynamicCache；转成逐层 (K, V) 元组后统计。
    if hasattr(cache, "to_legacy_cache"):
        cache = cache.to_legacy_cache()
    gib = tensor_bytes(cache) / (1024 ** 3)
    del outputs, cache, dummy_input
    return gib


for length in [128, 512, 2048]:
    kv_gib = measure_kv_cache(length)
    print(f"上下文长度 {length}: KV Cache 张量 {kv_gib:.3f} GiB")
# 确认设备余量后再测试更长序列，避免直接 OOM。
```

这里统计的是返回的 K/V 张量本身，而不是 `max_memory_allocated()`：后者还包含模型权重、Attention 临时张量、allocator 保留块等，不能当作 KV Cache 大小。实际缓存大小还取决于层数、KV head 数、head dimension、dtype 和 batch size；固定这些条件时通常随缓存序列长度近似线性增长。

---

# 八、（选读）为什么 MoE 更难在小显存单卡部署？

第 15 章讲过 MoE 的核心思想是“总参数多、每个 Token 只激活少数专家”。在**所有权重常驻单卡**的常见低延迟部署中，显存主要受总参数量约束，而每 Token 计算量更接近激活参数量；top-k 路由不会自动减少常驻权重。CPU/NVMe offload、专家并行或按需换入可以降低单卡常驻量，但会引入通信或传输开销。因此，MoE 通常比同等激活参数量的 Dense 模型更难在 8GB 单卡低延迟部署，具体占用仍取决于 checkpoint、量化格式和运行时。

如果只是想体会 Router 机制本身（而不需要真的跑一个大模型），可以用几行代码写一个最简化的 MoE 层，在 CPU 上就能运行：

```python
import torch
import torch.nn as nn

class ToyMoE(nn.Module):
    def __init__(self, d_model=8, num_experts=4, top_k=2):
        super().__init__()
        self.experts = nn.ModuleList(
            [nn.Linear(d_model, d_model) for _ in range(num_experts)]
        )
        self.router = nn.Linear(d_model, num_experts)
        self.top_k = top_k

    def forward(self, x):
        router_logits = self.router(x)                         # [batch, experts]
        topk_logits, topk_indices = torch.topk(
            router_logits, self.top_k, dim=-1
        )
        topk_weights = torch.softmax(topk_logits, dim=-1)     # top-k 内重新归一化

        all_outputs = torch.stack(
            [expert(x) for expert in self.experts], dim=1
        )                                                     # [batch, experts, d_model]
        gather_index = topk_indices.unsqueeze(-1).expand(-1, -1, x.shape[-1])
        chosen_outputs = torch.gather(all_outputs, 1, gather_index)
        mixed = (chosen_outputs * topk_weights.unsqueeze(-1)).sum(dim=1)
        return mixed, topk_indices, topk_weights


moe = ToyMoE(top_k=2)
x = torch.randn(1, 8)
output, expert_ids, router_weights = moe(x)
print("选中的专家:", expert_ids.tolist())
print("对应权重:", router_weights.tolist())
```

改变输入 `x`，可以观察 top-k 专家编号与归一化权重如何变化。为让代码短小，上例先计算全部专家再 `gather`；真实稀疏 MoE 内核只调度选中的专家，否则不会获得稀疏计算收益。

---

# 九、测量结果记录模板

不要跨平台照抄固定数值。运行实验后记录完整条件，才能解释差异：

| 后端与设备 | checkpoint / 量化格式 | Prompt / 新增 Token | KV Cache | 权重加载后占用 | 生成峰值占用 | 延迟 |
|---|---|---:|---|---:|---:|---:|
| CUDA + bitsandbytes | 请填写 | 请填写 | 开 / 关 | 请填写 | 请填写 | 请填写 |
| llama.cpp / Ollama | 请填写 | 请填写 | 开 | 请填写 | 请填写 | 请填写 |

FP16 权重 payload 可由参数量近似估算，但量化元数据、KV Cache、缓冲区、设备映射和 offload 都会改变运行时占用。MoE 也不应给出脱离部署条件的固定占用值，应按总/激活参数、量化格式和部署策略实测。

---

# 本节小结

✅ 区分了 CUDA bitsandbytes 与 macOS Metal/llama.cpp 路径 ✅ 用同步和分别预热对比 KV Cache 开关延迟 ✅ 直接统计 `past_key_values` 张量而非总峰值显存 ✅ 实现 top-k Router 权重归一化与专家输出加权 ✅ 明确 MoE 单卡常驻显存主要受总参数量约束，但可用 offload/专家并行换取空间

至此，第 15 章的全部实验都已完成——从 Next Token Prediction 到 Scaling Law，从思维链到量化部署，你已经走过了现代大语言模型从"训练"到"落地"的完整链路。

不过到这里，模型处理的始终只有文字。下一章 **第16章 多模态大模型——视觉信息编码与语言模型接入** 会把同一套 Embedding + Transformer 结构用到图像上。
