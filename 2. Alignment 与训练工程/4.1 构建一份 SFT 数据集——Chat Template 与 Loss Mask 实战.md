# 4.1 构建一份监督微调数据集——聊天模板（Chat Template）与损失掩码（Loss Mask）实战

> **硬件要求**：数据构建用 CPU 即可；第五节可选的 1.5B 模型前向传播还需要足够内存，不做反向训练。三个实验统一使用 `Qwen/Qwen2.5-1.5B` Base 与同系列 Instruct 的聊天模板，依赖版本见 4.2。3.1 清洗的预训练文本不会自动变成指令标注，这里从已有的指令—回答对开始。
>
> **本节目标**：3.1 节我们已经把原始文本清洗成了干净语料，4.2 节将用 LoRA 完整跑一次指令微调。夹在中间的这一节，专门拆解那个最容易黑箱化的环节：**一条 `{指令, 回答}` 的干净数据，究竟是怎么变成 Token、又是怎么变成 Loss Mask 的？为什么模型只学习"回答"，却不会学习"提问"？**

---

## 一、原始指令数据

```python
raw_data = [
    {"instruction": "介绍一下 Transformer。", "output": "Transformer 是一种基于 Self-Attention 的神经网络结构，由 Google 于 2017 年提出。"},
    {"instruction": "1+1等于几？", "output": "1+1等于2。"},
]
```

---

## 二、用 Chat Template 拼接成对话格式

Base tokenizer 不一定带聊天模板，也可能把 `<|endoftext|>` 而不是 `<|im_end|>` 设为 EOS。这里**明确从同系列 Instruct tokenizer 取模板与对话 EOS，而不是加载 Instruct 权重**；4.2 原样使用这个约定并保存 tokenizer，5.2 从产物重载它。

```python
from transformers import AutoTokenizer

MODEL_NAME = "Qwen/Qwen2.5-1.5B"
TEMPLATE_NAME = "Qwen/Qwen2.5-1.5B-Instruct"
SYSTEM = "你是一个中文助手，请准确回答并遵循用户的格式要求。"
tok = AutoTokenizer.from_pretrained(MODEL_NAME)
template_tok = AutoTokenizer.from_pretrained(TEMPLATE_NAME)
assert tok.get_vocab() == template_tok.get_vocab(), "模板与基座词表必须兼容"
assert template_tok.chat_template
tok.chat_template = template_tok.chat_template
tok.eos_token = template_tok.eos_token
tok.pad_token = template_tok.pad_token
tok.padding_side = "right"
assert tok.eos_token == "<|im_end|>"

def build_messages(sample):
    instruction = sample["instruction"].strip()
    extra = (sample.get("input") or "").strip()
    user_text = instruction + ("\n\n" + extra if extra else "")
    answer = sample["output"].strip()
    assert instruction and answer
    return [
        {"role": "system", "content": SYSTEM},
        {"role": "user", "content": user_text},
        {"role": "assistant", "content": answer},
    ]

sample = raw_data[0]
messages = build_messages(sample)
full_text = tok.apply_chat_template(messages, tokenize=False, add_generation_prompt=False)
print(full_text)
```

例如 `{"instruction": "翻译成英文", "input": "今天下雨了", "output": "It is raining today."}` 会把指令和附加输入一起放入 user 消息，不能漏掉 `input`。显式 system 消息避免依赖模板默认的角色描述。上面的对话结构为：

```text
<|im_start|>system
你是一个中文助手，请准确回答并遵循用户的格式要求。<|im_end|>
<|im_start|>user
介绍一下 Transformer。<|im_end|>
<|im_start|>assistant
Transformer 是一种基于 Self-Attention 的神经网络结构，由 Google 于 2017 年提出。<|im_end|>
```

---

## 三、为什么需要 Loss Mask？

如果直接把整段文本喂给模型做 Next Token Prediction，模型会**同时学习"如何提问"和"如何回答"**——但我们真正希望模型学会的只是"如何回答"。因此需要构造一个 `labels` 数组：**用户提问部分标记为 `-100`（PyTorch 的 `CrossEntropyLoss` 会自动忽略 `-100` 的位置），只保留 assistant 回答部分的真实 Token ID**。

```python
import torch

def build_training_sample(sample, tokenizer):
    messages = build_messages(sample)
    assert not any(t in m["content"] for t in tokenizer.all_special_tokens for m in messages)
    prompt = tokenizer.apply_chat_template(
        messages[:-1], tokenize=False, add_generation_prompt=True
    )
    full = tokenizer.apply_chat_template(
        messages, tokenize=False, add_generation_prompt=False
    )
    assert full.endswith(tokenizer.eos_token + "\n")
    full = full[:-1]
    prompt_ids = tokenizer(prompt, add_special_tokens=False)["input_ids"]
    full_ids = tokenizer(full, add_special_tokens=False)["input_ids"]
    assert full.startswith(prompt)
    assert full_ids[:len(prompt_ids)] == prompt_ids, "分词边界变化，不能用长度猜mask"
    answer_ids = full_ids[len(prompt_ids):]
    assert len(answer_ids) > 1 and answer_ids[-1] == tokenizer.eos_token_id
    labels = [-100] * len(prompt_ids) + answer_ids
    assert len(labels) == len(full_ids)
    return {"input_ids": full_ids, "attention_mask": [1] * len(full_ids), "labels": labels}

encoded = build_training_sample(raw_data[0], tok)
print("input_ids 长度:", len(encoded["input_ids"]))
print("被mask的token数:", sum(x == -100 for x in encoded["labels"]))
print("参与训练的token数:", sum(x != -100 for x in encoded["labels"]))
assert encoded["labels"][-1] == tok.eos_token_id
```

这里仅去掉模板在**最后一个 EOS 之后**追加的换行，不去掉 EOS。被监督的是 assistant 正文和回答结束符；system/user 内容与 assistant 开头标记都被 mask。EOS 也要学，否则模型可能不知道何时停止。CausalLM 内部会把 logits 与 labels 错开一位，**不要再手动 shift labels**。

这段实现刻意限定为单轮、纯文本、无内嵌特殊标记；多轮训练需要为每段 assistant 分别标出区间。样本暂不截断：4.2 先构造完整样本再过滤超长项，避免截掉全部回答或 EOS。`attention_mask=0` 只标记 padding，用户输入仍然必须可见。

批量训练还必须把 padding 位置的 `labels` 补成 `-100`。不要使用会以 `input_ids` 重建 labels 的通用语言模型 collator 覆盖已有 mask；4.2 使用保留 labels 的 `DataCollatorForSeq2Seq` 并断言 padding 不参与 Loss。

---

## 四、可视化：哪些 Token 参与了训练？

```python
# 把 Token ID 还原成可读的 Token 文本，逐个对照它是否参与了训练
tokens = tok.convert_ids_to_tokens(encoded["input_ids"])
labels = encoded["labels"]

for t, label in zip(tokens, labels):
    mark = "学习" if label != -100 else "忽略"
    print(f"{t:>15}  {mark}")
```

检查边界时尤其注意：assistant 开头标记被忽略，但它**后面的回答正文和末尾 `<|im_end|>`**参与训练。

---

## 五、用一个真实的前向传播验证（可选）

```python
from transformers import AutoModelForCausalLM

model = AutoModelForCausalLM.from_pretrained(MODEL_NAME)
model.eval()
batch = {k: torch.tensor([v], dtype=torch.long) for k, v in encoded.items()}
with torch.no_grad():
    output = model(**batch)
assert torch.isfinite(output.loss)
print("Loss:", output.loss.item())
```

能输出有限 Loss，说明模型接受了这个 batch；它**不能单独证明 mask 语义正确**，还要结合前缀、EOS、逐token检查和4.2的padding断言。Base 在刚引入的聊天格式上 Loss 可能较高，不应把它解释成模型已经会回答。

---

## 本节小结

- `instruction + 可选input → messages → Chat Template → input_ids/attention_mask/labels` 是本节的数据契约。
- prompt 前缀必须逐token一致，回答正文与 EOS 参与监督，padding 不参与监督。
- 本节提供可执行检查，不把未执行的模型前向宣称为实验结果；数据正确性还依赖样本质量与边界检查。

**SFT 的关键不只是选训练器，而是明确模型看见什么、哪些位置产生损失。**

到这里我们已经能造出标准的 SFT 样本了。数据准备好了，终于可以真正开始训练——下一节，我们用 LoRA 亲手把一个 Base 模型微调成能听懂指令的助手，把前面所有准备工作串成一次完整的实战：**4.2 使用 LoRA 微调一个模型——从 Base 到 Instruct**。
