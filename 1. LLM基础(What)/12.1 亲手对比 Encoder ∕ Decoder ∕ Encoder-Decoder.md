# 12.1 亲手对比 Encoder / Decoder / Encoder-Decoder

> **本节目标**：第12章讲了 Transformer 家族的三种架构——BERT（Encoder Only）、GPT（Decoder Only）、T5（Encoder-Decoder）。理论说它们分别擅长"理解"、"生成"、"信息变换"，但空说不如亲眼一见。本节，我们把三种真实的开源模型同时加载起来，用同一批输入分别喂给它们，亲手体会三种架构在行为上的本质差异。
>
> **硬件要求**：三个模型都是"小号"版本，全部加载同时占用显存不到 2GB，**8GB 显存随便一张卡都能同时跑三个，甚至 CPU 也完全没问题**。

---

# 一、环境准备与模型选择

| 架构 | 模型 | 参数量 | 作用 |
|---|---|---|---|
| Encoder Only | `bert-base-chinese` | ~1亿 | 理解、填空 |
| Decoder Only | `Qwen/Qwen2.5-0.5B-Instruct` | ~5亿 | 续写、生成 |
| Encoder-Decoder | `google/mt5-small` | ~3亿 | 摘要、翻译 |

```bash
pip install torch transformers sentencepiece
```

---

# 二、体验 Encoder（BERT）：擅长"填空"，不擅长"续写"

BERT 训练时用的是"完形填空"（Masked Language Model），因此天生适合"给你一句话缺一个词，猜出缺的是什么"：

```python
from transformers import AutoTokenizer, AutoModelForMaskedLM
import torch

bert_name = "bert-base-chinese"
bert_tok = AutoTokenizer.from_pretrained(bert_name)
bert_model = AutoModelForMaskedLM.from_pretrained(bert_name)
bert_model.eval()

text = "北京是中国的[MASK]都。"
inputs = bert_tok(text, return_tensors="pt")
mask_pos = (inputs["input_ids"][0] == bert_tok.mask_token_id).nonzero(as_tuple=True)[0].item()

with torch.no_grad():
    logits = bert_model(**inputs).logits

top5 = torch.topk(logits[0, mask_pos], k=5).indices
print("BERT 填空结果:", [bert_tok.decode([t]) for t in top5])
```

试试把 `text` 换成 `"我今天心情很[MASK]。"`，你会发现 BERT 能利用**左右两侧**的信息一起判断，这正是第12章讲的**双向 Attention**。

现在试试让 BERT"续写"一句话——你会发现它根本不支持这种调用方式，因为它从来没有学习过"一个词一个词往后生成"，这正是 Encoder 的天生局限。

---

# 三、体验 Decoder（GPT/Qwen）：擅长"续写"，看不到未来

```python
from transformers import AutoTokenizer, AutoModelForCausalLM

gpt_name = "Qwen/Qwen2.5-0.5B-Instruct"
gpt_tok = AutoTokenizer.from_pretrained(gpt_name)
gpt_model = AutoModelForCausalLM.from_pretrained(gpt_name, torch_dtype=torch.float16)
gpt_model.eval()

prompt = "北京是中国的"
inputs = gpt_tok(prompt, return_tensors="pt")
output = gpt_model.generate(**inputs, max_new_tokens=20, do_sample=False)
print("GPT 续写结果:", gpt_tok.decode(output[0], skip_special_tokens=True))
```

Decoder 天生就能一个词一个词往后生成，但如果你问它"这句话前面缺了什么词"，它做不到——因为 Causal Mask 决定了它**只能看左边，不能看右边**，这正是第12章讲的核心限制。

---

# 四、体验 Encoder-Decoder（T5）：先理解，再重新生成

T5 类模型习惯用"任务前缀"告诉模型要做什么任务：

```python
from transformers import AutoTokenizer, AutoModelForSeq2SeqLM

t5_name = "google/mt5-small"
t5_tok = AutoTokenizer.from_pretrained(t5_name)
t5_model = AutoModelForSeq2SeqLM.from_pretrained(t5_name)
t5_model.eval()

text = "summarize: The Transformer architecture, introduced in 2017, relies entirely on attention mechanisms and has become the foundation of almost all modern large language models."
inputs = t5_tok(text, return_tensors="pt")
output = t5_model.generate(**inputs, max_new_tokens=30)
print("T5 摘要结果:", t5_tok.decode(output[0], skip_special_tokens=True))
```

> 提示：`mt5-small` 没有针对摘要任务专门微调过，效果可能比较粗糙——这里的重点不是"效果好不好"，而是**体会它的调用方式和输出方式与 BERT、GPT 完全不同**：输入先经过 Encoder 完整理解一遍，再由 Decoder 从零开始生成一段**全新的**输出序列，而不是像 GPT 那样在原文后面接着写。

---

# 五、三种架构一张表看懂

| | 输入方式 | 输出方式 | 能否看到完整上下文 | 典型任务 |
|---|---|---|---|---|
| BERT（Encoder） | 一次性输入整句话 | 对某个位置输出一个类别/词 | ✅ 双向 | 分类、填空、检索 |
| GPT（Decoder） | 已生成的历史 Token | 逐个 Token 向后生成 | ❌ 只能看左边 | 续写、聊天、代码生成 |
| T5（Encoder-Decoder） | 完整输入序列 | Decoder 从零生成全新序列 | Encoder 端双向，Decoder 端只能看左边 | 翻译、摘要、改写 |

---

# 六、同一个句子，三个模型的"性格"对比

用同一个输入 `"人工智能正在改变世界。"` 分别调用三个模型（BERT 做 `[MASK]` 填空、GPT 做续写、T5 做 `translate to English:` 前缀翻译），把三个输出打印在一起对比，你会非常直观地感受到：**同样是 Transformer，三种架构的"性格"完全不同**——BERT像一位阅读理解专家，GPT像一位健谈的续写者，T5像一位专业翻译。这正是第12章反复强调的核心观点：**没有最好的 Transformer，只有最适合任务的 Transformer。**

---

# 本节小结

✅ 同时加载并运行了 BERT、GPT、T5 三种真实的开源模型 ✅ 亲手验证了 BERT 能填空但不能续写、GPT 能续写但看不到未来、T5 需要任务前缀且输出全新序列 ✅ 三个模型总显存占用不到 2GB，8GB 显卡或 CPU 均可轻松完成 ✅ 直观理解了"架构决定能力边界"这一核心思想

下一章，我们将进入本书最后一个理论章节——大语言模型（LLM），学习 Next Token Prediction、Scaling Law、Instruction Tuning、KV Cache 等现代 LLM 的核心技术，并在 13.1~13.6 节通过一系列本地部署实验，亲手体验从"训练"到"微调"再到"量化推理加速"的完整技术栈。
