# Chapter 3：Embedding —— 大模型如何理解 Token？

> **本章目标**
>
> 第二章中，我们实现了一个完整的 Tokenizer。
>
> 它已经能够把：
>
> ```
> Hello World
> ```
>
> 转换成：
>
> ```
> [15496, 995]
> ```
>
> 但是新的问题来了：
>
> **这些数字到底代表什么意思？**
>
> Token ID 是不是已经包含了单词的含义？
>
> 本章将回答整个 Transformer 的第一个核心问题：
>
> > **神经网络为什么不能直接处理 Token ID？**
>
> 并从零实现一个最小版 Embedding Layer。

---

# 本章学习目标

完成本章后，你应该能够回答下面几个问题：

- 为什么 Token ID 不能直接输入神经网络？
- 什么是 One-Hot Encoding？
- 什么是 Embedding？
- Embedding Matrix 是什么？
- 为什么叫 Lookup Table？
- Embedding 为什么会不断训练？
- 相似单词为什么距离会越来越近？
- GPT 的第一层到底做了什么？

---

# 3.1 第二章留下的问题

第二章最后。

Tokenizer 已经完成：

```
Text

↓

Tokenizer

↓

Token IDs
```

例如：

```
I love AI
```

编码：

```
[2,3,1]
```

这里：

很多初学者都会觉得：

```
2

3

1
```

已经能够表示：

```
I

love

AI
```

事实上：

完全不是。

---

# 3.2 Token ID 没有任何语义

观察下面两个词：

```
cat
```

```
dog
```

Tokenizer：

可能得到：

```
cat

↓

128
```

```
dog

↓

129
```

很多人会误以为：

```
129

>

128
```

是不是说明：

```
dog

比

cat

更大？
```

当然不是。

Token ID：

只是：

> **Vocabulary 中的编号。**

就像：

学生学号：

```
1001

1002

1003
```

学号之间：

没有任何数学意义。

Token ID：

也是一样。

---

# 再看一个例子

假设：

Vocabulary：

```
{
apple:0,

banana:1,

car:2,

computer:3
}
```

是不是：

```
computer

>

banana
```

没有任何意义？

因为：

```
0

1

2

3
```

只是：

编号。

而不是：

知识。

所以：

神经网络不能直接学习：

```
3

>

2

>

1
```

这样的关系。

---

# 3.3 神经网络真正需要什么？

神经网络真正擅长处理的是：

```
向量（Vector）
```

例如：

```
[0.13

0.42

0.77

...]
```

为什么？

因为：

向量之间。

可以计算：

```
距离

方向

相似度

线性组合
```

这些都是神经网络能够学习的。

因此：

真正的数据流。

应该变成：

```
Text

↓

Tokenizer

↓

Token IDs

↓

Embedding

↓

Vectors

↓

Transformer
```

请注意。

Embedding：

就是：

连接：

```
离散 Token

↓

连续向量
```

之间的桥梁。

---

# 3.4 本章知识地图

```
Human Text
      │
      ▼
Tokenizer
      │
      ▼
Token IDs
      │
      ▼
Embedding
      │
      ▼
Dense Vectors
      │
      ▼
Neural Network
```

这里开始。

整个模型。

第一次进入：

> **连续空间（Continuous Space）**

也是现代 AI 与传统程序最大的区别。

---

# 本章实验安排

本章依旧遵循**代码演进**原则，不会重写已有代码。

我们将在第二章 Tokenizer 的基础上，增加一个新的模块：

```
chapter03/
│
├── embedding.py
├── experiment01.py
├── experiment02.py
└── experiment03.py
```

四个实验如下：

| 实验 | 内容 | 工程演进 |
|------|------|----------|
| **3.1** | One-Hot Encoding | 第一次把 Token ID 转成向量 |
| **3.2** | Embedding Matrix | 用矩阵替代 One-Hot |
| **3.3** | 训练 Embedding | 理解向量为什么会越来越"懂语义" |
| **3.4** | 可视化 Embedding | 观察相似词聚集现象 |

---

# 本章最终工程

完成本章后，我们的工程将演进为：

```
Text
 │
 ▼
Tokenizer
 │
 ▼
Token IDs
 │
 ▼
Embedding
 │
 ▼
Vectors
 │
 ▼
Language Model
```

这也是现代 GPT 在进入 Transformer Block 之前的完整数据流。

---

# 本章一句话总结

前两章解决的是：

> **如何把文字变成 Token。**

第三章解决的是：

> **如何把 Token 变成神经网络能够理解的向量。**

从这里开始，我们真正进入了**深度学习**。