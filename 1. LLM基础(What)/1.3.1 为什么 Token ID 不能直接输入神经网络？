很好，我们继续。

按照我们冻结的写作风格，**第三章也遵循"提出问题→失败方案→演进方案"**。

所以第一节不能直接讲 Embedding Matrix，而是应该先让读者自己意识到：

> **Token ID 根本不能直接输入神经网络。**

---

# 实验 3.1：为什么 Token ID 不能直接输入神经网络？

> **实验目标**
>
> 第二章中，我们已经能够把文本转换成 Token ID。
>
> 例如：
>
> ```text
> I love AI
> ```
>
> Tokenizer 输出：
>
> ```text
> [2, 3, 1]
> ```
>
> 那么，一个很自然的问题来了：
>
> **为什么不能直接把 `[2,3,1]` 输入神经网络？**
>
> 本实验将通过几个简单实验，理解为什么神经网络无法直接学习 Token ID，并自然推导出 One-Hot Encoding。

---

# 一、回顾第二章

目前我们的整个工程如下：

```text
Text
 │
 ▼
Tokenizer
 │
 ▼
Token IDs
```

例如：

```text
I love AI
```

经过 Tokenizer：

```text
I      → 2

love   → 3

AI     → 1
```

最终得到：

```python
[2,3,1]
```

很多初学者第一次都会产生一个想法：

> **既然已经是数字了，那为什么不能直接送进神经网络？**

这是一个非常好的问题。

---

# 二、做一个最简单的实验

假设 Vocabulary 如下：

| Token  | ID |
| ------ | -: |
| apple  |  0 |
| banana |  1 |
| cat    |  2 |
| dog    |  3 |

如果直接输入神经网络：

```python
apple  -> 0

banana -> 1

cat    -> 2

dog    -> 3
```

看起来没有问题。

但是观察一下这些数字。

它们隐含着一种数学关系：

```text
3 > 2 > 1 > 0
```

甚至：

```text
dog - cat = 1

cat - banana = 1
```

请问：

这些关系是真的吗？

当然不是。

---

# 三、Token ID 只是编号

我们换一个角度。

假设学校里：

```text
张三

↓

1001
```

```text
李四

↓

1002
```

```text
王五

↓

1003
```

难道：

```text
1003

>

1002
```

说明：

王五：

比李四：

"更大"？

当然不是。

学号只是：

> **唯一编号（Identifier）**

没有任何数学意义。

Token ID：

也是一样。

它只是：

Vocabulary 中的位置。

---

# 四、修改编号会发生什么？

继续实验。

原来的 Vocabulary：

```text
apple -> 0

banana -> 1

cat -> 2

dog -> 3
```

现在。

我们重新排序：

```text
dog -> 0

cat -> 1

banana -> 2

apple -> 3
```

请问：

语言发生变化了吗？

没有。

真正变化的只有：

```text
编号
```

说明：

Token ID：

本身没有任何语义。

---

# 五、神经网络真正需要什么？

神经网络能够学习的是：

```text
连续空间（Continuous Space）
```

例如：

```text
[0.15,0.72,-0.31]
```

而不是：

```text
3
```

为什么？

因为：

连续向量之间：

可以计算：

* 欧氏距离（Euclidean Distance）
* 余弦相似度（Cosine Similarity）
* 点积（Dot Product）
* 方向（Direction）

例如：

```text
cat

↓

[0.21,0.34,-0.55]
```

```text
dog

↓

[0.20,0.37,-0.52]
```

两个向量非常接近。

于是模型可以学习：

```text
cat

≈

dog
```

但是：

如果只有：

```text
2

3
```

模型无法知道：

为什么：

2 和 3：

比：

2 和 100：

更接近。

因为：

编号本身没有意义。

---

# 六、第一个解决方案：One-Hot Encoding

既然：

```text
2
```

不能直接输入。

那么：

有没有一种方式。

能够：

保留：

Token 的唯一身份。

又不会引入：

大小关系？

答案就是：

> **One-Hot Encoding**

例如：

Vocabulary：

```text
apple

banana

cat

dog
```

One-Hot：

```text
apple

↓

[1,0,0,0]
```

```text
banana

↓

[0,1,0,0]
```

```text
cat

↓

[0,0,1,0]
```

```text
dog

↓

[0,0,0,1]
```

请注意。

所有 Token：

都是：

等距离。

不存在：

```text
dog > cat
```

这种错误关系。

---

# 七、为什么叫 One-Hot？

观察：

```text
[0,0,1,0]
```

只有：

一个位置：

是：

```text
1
```

其它全部：

```text
0
```

因此：

叫：

```text
One-Hot
```

意思就是：

> **只有一个位置是激活（Hot）的。**

---

# 八、完整数据流

到这里。

整个系统已经演进成：

```text
Text
 │
 ▼
Tokenizer
 │
 ▼
Token ID
 │
 ▼
One-Hot
 │
 ▼
Neural Network
```

是不是已经比：

```text
Token ID

↓

Neural Network
```

合理很多？

---

# 九、One-Hot 真的是最终方案吗？

请思考：

GPT 的 Vocabulary：

大约：

```text
100,000 Token
```

如果：

每个 Token：

都是：

One-Hot。

那么：

一个 Token：

就是：

```text
100000维
```

向量。

是不是非常浪费？

事实上。

现代 GPT：

**几乎不会真正生成 One-Hot 向量。**

它采用了一种更加高效的方法：

> **Embedding Matrix**

下一节。

我们就会发现：

Embedding Matrix。

其实就是：

One-Hot 的一种优化实现。

---

# 实验代码

这一节代码非常简单。

```python
vocab = ["apple", "banana", "cat", "dog"]

token_id = 2

one_hot = [0] * len(vocab)

one_hot[token_id] = 1

print(one_hot)
```

输出：

```text
[0,0,1,0]
```

再测试：

```python
token_id = 0

one_hot = [0] * len(vocab)

one_hot[token_id] = 1

print(one_hot)
```

输出：

```text
[1,0,0,0]
```

---

# 实验收获（Takeaways）

✅ 理解 Token ID 只是编号，没有语义。

✅ 理解神经网络需要连续向量，而不是离散编号。

✅ 理解 One-Hot Encoding 的设计目的。

✅ 理解 One-Hot 消除了 Token ID 的大小关系。

❌ One-Hot 维度过高，不适合现代大模型。

---

# Code Evolution

```text
Text
 │
 ▼
Tokenizer
 │
 ▼
Token IDs
 │
 ▼
One-Hot
```

下一节，我们将完成第三章最关键的一次演进：

```text
One-Hot
      │
      ▼
Embedding Matrix
```

你会发现，Embedding Matrix 并不是一种全新的东西，它本质上就是 **One-Hot 的高效实现**。这也是现代 Transformer 第一层真正执行的操作。
