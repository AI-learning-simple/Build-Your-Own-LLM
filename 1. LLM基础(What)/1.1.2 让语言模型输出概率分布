# 实验 1.2：让 Bigram 输出概率分布

> **实验目标**

上一节，我们实现了一个 Character Bigram Language Model。

它已经能够生成文本，但是仍然存在一个问题：

当模型看到一个字符时，它并不知道：

> **每个候选字符出现的概率是多少。**

例如：

```
Current Character：

l
```

模型知道：

```
l

↓

l
o
d
```

但是：

```
l 出现多少次？

o 出现多少次？

d 出现多少次？
```

完全不知道。

本实验将把 Bigram 升级成：

> **Probability Language Model（概率语言模型）**

---

# 一、为什么需要概率？

观察下面两个句子：

```
I love AI
```

```
I like AI
```

假设：

训练数据里面：

```
I love
```

出现：

600 次。

而：

```
I like
```

出现：

400 次。

如果输入：

```
I
```

模型应该输出：

```
love
```

还是：

```
like
```

其实。

正确答案不是：

```
love
```

而是：

| Next Word | Probability |
|------------|------------:|
| love | 60% |
| like | 40% |

这就是：

Language Model 真正应该输出的东西。

---

# 二、统计出现次数（Frequency）

上一节。

我们的模型保存的是：

```python
{
'l':['l','o','d']
}
```

这种结构不方便统计次数。

现在修改成：

```python
{
'l':{
    'l':1,
    'o':1,
    'd':1
}
}
```

这样：

每一种下一个字符。

都有自己的计数。

---

# 三、修改训练代码

把上一节：

```python
model[current].append(next)
```

修改为：

```python
if current not in model:

    model[current] = {}

if nxt not in model[current]:

    model[current][nxt] = 0

model[current][nxt] += 1
```

运行：

```
{
'h':{'e':1},
'e':{'l':1},
'l':{'l':1,'o':1,'d':1},
'o':{' ':1,'r':1},
...
}
```

可以发现。

已经不再保存列表。

而是：

保存：

> **出现次数。**

---

# 四、计算概率

例如：

```
l
```

对应：

```
{
'l':1,
'o':1,
'd':1
}
```

总次数：

```
3
```

因此：

```
P(l|l)=1/3

P(o|l)=1/3

P(d|l)=1/3
```

如果：

训练数据改成：

```
helllllllo world
```

那么：

```
{
'l':{
'l':6,
'o':1,
'd':1
}
}
```

概率：

| Next | Probability |
|------|------------:|
| l | 75% |
| o | 12.5% |
| d | 12.5% |

是不是已经越来越像 GPT？

---

# 五、新增 predict() 函数

增加：

```python
def predict(current):

    if current not in model:

        return

    table = model[current]

    total = sum(table.values())

    print(f"\nCurrent: {current}")

    print("-"*25)

    for nxt,count in sorted(
            table.items(),
            key=lambda x:-x[1]
        ):

        prob = count / total

        print(f"{nxt:<5} {prob:.2%}")
```

运行：

```python
predict("l")
```

输出：

```
Current: l

-------------------------

l      33.33%

o      33.33%

d      33.33%
```

如果：

输入：

```python
predict("o")
```

输出：

```
Current: o

-------------------------

(space)   50%

r         50%
```

这里第一次真正看到：

> **Probability Distribution（概率分布）**

---

# 六、完整代码

```python
text = "hello world"

model = {}

for i in range(len(text)-1):

    current = text[i]
    nxt = text[i+1]

    if current not in model:
        model[current] = {}

    if nxt not in model[current]:
        model[current][nxt] = 0

    model[current][nxt] += 1


def predict(current):

    if current not in model:
        return

    table = model[current]

    total = sum(table.values())

    print(f"\nCurrent: {current}")

    print("-"*25)

    for nxt,count in sorted(
            table.items(),
            key=lambda x:-x[1]
        ):

        print(
            f"{repr(nxt):<8}"
            f"{count/total:.2%}"
        )


predict("l")
predict("o")
```

---

# 七、运行结果

```
Current: l

-------------------------

'l'     33.33%

'o'     33.33%

'd'     33.33%



Current: o

-------------------------

' '     50.00%

'r'     50.00%
```

---

# 八、实验分析

到目前为止。

我们的 Language Model 已经具备了两个能力：

第一。

能够统计：

```
Current Character

↓

Next Character
```

第二。

能够输出：

```
Probability Distribution
```

也就是说。

它已经真正成为了：

> **Language Model。**

虽然非常简单。

但是：

现代 GPT。

最后输出的。

本质上仍然是：

```
Token

↓

Probability
```

不同的是。

GPT 的概率。

不是靠统计。

而是靠：

神经网络学习得到。

后面的 Transformer。

Embedding。

Attention。

都是为了：

学习一个更准确的：

Probability Distribution。

---

# 九、本实验修改了什么？

相比实验 1.1。

只修改了两处。

第一。

模型：

```
List
```

升级成：

```
Dictionary
```

第二。

新增：

```
predict()
```

能够输出：

```
Probability Distribution
```

Language Model 的主体。

完全没有变化。

---

# 实验收获（Takeaways）

✅ 学会了统计下一个字符出现的次数。

✅ 学会了计算条件概率。

✅ 理解了什么是 Probability Distribution。

✅ 理解了为什么 Language Model 输出的是概率，而不是答案。

---

> **下一实验不会继续修改 Language Model。**

下一章，我们会开始构建 **Tokenizer**。

你会发现：

第一章的模型一直直接处理字符：

```
h

↓

e

↓

l
```

而真正的 GPT，从来不会直接处理字符。

它真正处理的是：

```
Token ID
```

因此，第二章我们在它前面增加一个 **Tokenizer**，让整个工程第一次真正接近现代 LLM。