# 实验 3.3：Embedding 是如何学习语义的？

> **实验目标**
>
> 上一节，我们实现了 Embedding Matrix。
>
> 例如：
>
> ```
> cat
> ↓
> [0.91,-0.18]
> ```
>
> 但是：
>
> **这些数字完全是随机的。**
>
> 那么：
>
> 为什么训练结束以后：
>
> ```
> cat
> ```
>
> 和：
>
> ```
> dog
> ```
>
> 的向量会越来越接近？
>
> 本实验将用一个极简例子，观察 Embedding 是如何一步一步学习出来的。

---

# 一、随机初始化

上一节。

我们的 Embedding：

其实是：

```python
embedding = [

    [0.12,0.83],      # apple

    [-0.55,0.27],     # banana

    [0.91,-0.18],     # cat

    [0.88,-0.22]      # dog

]
```

这些数字：

没有任何意义。

实际上。

GPT 初始化时也是这样。

例如：

```
cat

↓

[-0.013

0.004

0.019

...]
```

全部都是：

随机数。

所以：

第一步：

> **Embedding 没有语义。**

---

# 二、模型什么时候知道 "cat" 和 "dog" 很像？

观察下面两句话：

```
The cat is sleeping.
```

```
The dog is sleeping.
```

训练过程中。

模型会不断看到：

```
cat

↓

is

↓

sleeping
```

以及：

```
dog

↓

is

↓

sleeping
```

它会慢慢发现：

```
cat
```

和：

```
dog
```

总是出现在：

相似的位置。

于是：

模型开始调整：

```
Embedding(cat)
```

和：

```
Embedding(dog)
```

让它们越来越接近。

请注意。

这里：

没有人告诉模型：

```
cat≈dog
```

模型完全依赖：

> **上下文（Context）**

自己学习。

---

# 三、什么叫 "学习"？

我们用一个非常简单的例子。

假设：

```
cat
```

当前 Embedding：

```text
[0.90,-0.20]
```

模型预测：

```
"The cat is"

↓

running
```

但是。

正确答案应该是：

```
sleeping
```

于是：

模型会得到一个：

```
Prediction Error（预测误差）
```

例如：

```
Loss = 0.82
```

Loss：

越大。

说明：

预测越差。

---

# 四、Loss 会驱动参数更新

模型发现：

```
Loss 很大
```

于是：

开始调整：

```
cat
```

对应的向量。

例如：

第一次：

```
[0.90,-0.20]
```

第二次：

```
[0.86,-0.17]
```

第三次：

```
[0.82,-0.12]
```

第四次：

```
[0.79,-0.09]
```

请注意。

模型并不知道：

正确答案是什么向量。

它只是：

不断尝试：

```
修改一点点

↓

Loss 是否下降？
```

如果：

Loss：

下降。

继续这个方向。

否则：

换方向。

---

# 五、什么是 Gradient（梯度）？

这里。

第一次出现：

Gradient。

很多教材会直接给公式。

其实。

一句话就够了：

> **梯度告诉模型：参数应该往哪个方向修改，Loss 才会下降。**

例如：

```
Embedding(cat)

↓

[0.90,-0.20]
```

梯度告诉模型：

```
第一维：

减小一点。

第二维：

增大一点。
```

于是：

新的 Embedding：

```
[0.88,-0.17]
```

如果：

Loss：

真的下降。

说明：

方向是正确的。

---

# 六、什么叫 Gradient Descent？

整个训练过程。

就是不断重复：

```
Forward

↓

Loss

↓

Gradient

↓

Update

↓

Forward

↓

Loss

↓

Gradient

↓

Update
```

直到：

Loss：

越来越小。

这就是：

> **Gradient Descent（梯度下降）**

为什么叫：

下降？

因为：

Loss：

越来越低。

像：

不断下山。

---

# 七、代码模拟一次更新

下面。

我们不用真正计算梯度。

只是模拟：

一次参数更新。

```python
embedding = [0.90,-0.20]

gradient = [0.05,-0.03]

learning_rate = 0.1

new_embedding = [

    embedding[i] -

    learning_rate * gradient[i]

    for i in range(2)

]

print(new_embedding)
```

输出：

```
[0.895,-0.197]
```

这里：

虽然变化很小。

但是：

真正 GPT：

会进行：

数十亿次。

这样的更新。

---

# 八、训练很多次以后

训练开始：

```
cat

↓

[0.91,-0.18]
```

训练结束：

```
cat

↓

[0.27,0.74]
```

与此同时：

```
dog

↓

[0.25,0.70]
```

于是：

两个向量：

越来越近。

请注意。

这里：

没有任何人：

手工设计：

```
cat≈dog
```

模型：

完全根据：

大量文本。

自己学习出来。

---

# 九、真正发生变化的是 Embedding Matrix

很多人第一次学习 GPT。

都会觉得：

Embedding：

只是：

查表。

其实。

真正训练过程中。

一直变化的是：

```
Embedding Matrix
```

例如：

训练开始：

| Token | Vector |
|------|----------------|
| cat | [0.91,-0.18] |
| dog | [0.33,0.11] |
| apple | [-0.72,0.48] |

训练结束：

| Token | Vector |
|------|----------------|
| cat | [0.27,0.74] |
| dog | [0.25,0.70] |
| apple | [-0.81,-0.32] |

所以：

Embedding：

真正的本质是：

> **一个可以训练的参数矩阵（Trainable Parameters）。**

---

# 实验收获（Takeaways）

✅ Embedding 初始只是随机数。

✅ Loss 衡量预测好坏。

✅ Gradient 指出参数更新方向。

✅ Gradient Descent 不断降低 Loss。

✅ Embedding 的语义来自训练，而不是人工设计。

---

# Code Evolution

```
Text
 │
 ▼
Tokenizer
 │
 ▼
Token IDs
 │
 ▼
Embedding Matrix
 │
 ▼
Gradient Descent
 │
 ▼
Semantic Embedding
```

---

# Design Discussion（一个非常重要的问题）

到这里，我们已经知道：

```
cat

↓

[0.27,0.74]
```

但是：

模型真正利用的不是某一个 Token。

而是：

一句话：

```
The

cat

is

sleeping
```

请问：

模型如何知道：

> **"cat" 前面是 "The"，后面是 "is sleeping"？**

Embedding 本身只能表示：

**单个 Token**。

它完全不知道：

上下文。

所以：

新的问题出现了：

> **如何让一个 Token 感知其它 Token？**

这正是 Transformer 诞生之前，整个 NLP 领域几十年一直在解决的问题。

下一章，我们不会马上进入 Transformer，而是会先实现一个**最简单的 Neural Language Model（MLP Language Model）**。这样读者会自然发现：**Embedding 有了，但模型仍然不会利用上下文**，从而顺理成章引出 Self-Attention。这样第四章、第五章和第六章之间的逻辑链会非常自然，而不会出现"突然开始讲 Attention"的跳跃。