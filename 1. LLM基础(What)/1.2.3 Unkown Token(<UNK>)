我赞同继续完善第二章。

不过，我想在这里**冻结一个比原来更好的设计**。随着我们已经写到第二章，我发现我们的目标已经不是"教 Tokenizer"，而是**让读者自己推导出为什么 GPT 最终选择 BPE**。

所以后面的章节，我建议不要直接介绍算法，而是采用**"提出问题 → 尝试解决 → 发现新问题 → 再演进"**的方式。

例如：

```
Character
    │
    ▼
序列太长
    │
    ▼
Word
    │
    ▼
OOV（新词）
    │
    ▼
<UNK>
    │
    ▼
信息丢失
    │
    ▼
Subword
    │
    ▼
如何自动切？
    │
    ▼
BPE
    │
    ▼
现代 GPT Tokenizer
```

这比直接讲 BPE 更符合工程演进思路。

---

# 实验 2.3：Unknown Token（<UNK>）

> **实验目标**
>
> 上一节，我们实现了 Word Tokenizer。
>
> 它比 Character Tokenizer 更高效，但却暴露出了一个致命问题：
>
> **遇到词表中不存在的新词时，程序会直接崩溃。**
>
> 本实验将引入 NLP 中第一个特殊 Token：
>
> **`<UNK>`（Unknown Token）**。

---

# 一、回顾上一节的问题

上一节的代码：

```python
tokenizer = WordTokenizer("I love AI")

tokenizer.encode("I love ChatGPT")
```

程序直接报错：

```text
KeyError: 'ChatGPT'
```

原因很简单。

训练数据中只有：

```
I
love
AI
```

因此 Vocabulary 为：

```text
{
AI:0,
I:1,
love:2
}
```

当 Tokenizer 遇到：

```
ChatGPT
```

它不知道应该映射成哪个 Token ID。

这就是：

> **Out-of-Vocabulary（OOV）**

也就是：

> **词表之外的单词。**

---

# 二、最直接的解决办法

既然不知道是什么词。

那就统一表示成：

```
<UNK>
```

意思就是：

> **Unknown（未知）**

于是 Vocabulary 变成：

```text
{
<UNK>:0,
AI:1,
I:2,
love:3
}
```

以后。

所有不认识的词。

全部映射成：

```
0
```

是不是就不会崩溃了？

---

# 三、升级 build()

修改：

```python
vocab = sorted(set(words))
```

变成：

```python
vocab = ["<UNK>"] + sorted(set(words))
```

完整代码：

```python
def build(self, text):

    words = text.split()

    vocab = ["<UNK>"] + sorted(set(words))

    self.stoi = {
        w: i
        for i, w in enumerate(vocab)
    }

    self.itos = {
        i: w
        for w, i in self.stoi.items()
    }
```

Vocabulary：

变成：

```text
{
<UNK>:0,
AI:1,
I:2,
love:3
}
```

---

# 四、升级 encode()

上一节：

```python
return [

    self.stoi[word]

    for word in text.split()

]
```

如果：

不存在：

程序就会崩溃。

改成：

```python
def encode(self, text):

    ids = []

    for word in text.split():

        ids.append(

            self.stoi.get(
                word,
                self.stoi["<UNK>"]
            )

        )

    return ids
```

这里第一次使用：

```python
dict.get()
```

如果：

找到：

返回真正 Token ID。

如果：

找不到：

返回：

```
<UNK>
```

---

# 五、测试

训练：

```python
tokenizer = WordTokenizer(

    "I love AI"

)
```

Vocabulary：

```text
{
<UNK>:0,
AI:1,
I:2,
love:3
}
```

编码：

```python
print(

    tokenizer.encode(

        "I love ChatGPT"

    )

)
```

输出：

```text
[2,3,0]
```

解释：

```
I

↓

2

love

↓

3

ChatGPT

↓

<UNK>

↓

0
```

程序终于不会崩溃了。

---

# 六、再测试一句话

输入：

```python
I love OpenAI
```

编码：

```text
[2,3,0]
```

输入：

```python
I love Claude
```

编码：

```text
[2,3,0]
```

输入：

```python
I love Gemini
```

编码：

```text
[2,3,0]
```

你会发现：

所有未知词。

全部变成：

```
0
```

---

# 七、真的解决问题了吗？

很多教程到这里就结束了。

但是，我们继续思考。

假设：

下面三句话：

```
I love ChatGPT

I love Claude

I love Gemini
```

经过 Tokenizer。

全部变成：

```text
I love <UNK>
```

也就是：

```text
[2,3,0]
```

对于模型来说：

```
ChatGPT

Claude

Gemini
```

已经没有任何区别。

所有信息：

全部丢失了。

---

# 八、为什么 `<UNK>` 不是最终方案？

这是很多 NLP 初学者都会疑惑的问题。

`<UNK>` 的确解决了：

> **程序不会崩溃。**

但是：

它带来了新的问题。

例如：

```
OpenAI

ChatGPT

GPT-5

Claude

Gemini
```

全部变成：

```
<UNK>
```

模型根本不知道：

这些其实是不同的词。

因此：

虽然：

Word Tokenizer + `<UNK>`

已经能够正常运行。

但是：

它无法处理：

> **无限增长的新词。**

现代 LLM：

几乎都已经放弃：

```
<UNK>
```

而采用：

```
Subword
```

也就是：

把：

```
ChatGPT
```

切成：

```
Chat

GPT
```

即使：

整个词没见过。

模型也认识：

```
Chat

GPT
```

于是：

自然能够理解：

```
ChatGPT
```

---

# 实验收获（Takeaways）

✅ 学会了 `<UNK>` 的设计思想。

✅ 理解了 OOV（Out-of-Vocabulary）。

✅ 学会用 `dict.get()` 处理未知词。

❌ `<UNK>` 会丢失所有未知词的信息。

---

# 代码演进图

```text
Character Tokenizer
        │
        ▼
Word Tokenizer
        │
        ▼
+ <UNK>
        │
        ▼
程序不会崩溃
        │
        ▼
但是信息全部丢失
```

---

# Design Discussion（为什么不直接停在 `<UNK>`？）

请思考下面两句话：

```
I love ChatGPT
I love Claude
```

经过 `<UNK>` 后：

```
I love <UNK>
I love <UNK>
```

模型已经无法区分两句话。

所以，我们真正需要的不是：

> **"把未知词变成同一个 Token"**

而是：

> **"把未知词拆成模型认识的更小单元。"**

这就是下一节 **Subword Tokenizer** 的核心思想。

---

## 我建议下一节不要直接讲 BPE，而是先讲 **Subword**。

这是很多教材容易跳过的一步，但我认为非常关键。

因为 **BPE 并不是一种目标，它只是实现 Subword 的一种算法**。

如果先理解：

> Character 太细 → Word 太粗 → 我们需要一个介于两者之间的单位（Subword）

那么 BPE 就会变成一个水到渠成的工程方案，而不是一个需要死记硬背的新算法。这样整本书的逻辑会更加完整，也更符合我们一直坚持的"从问题推导方案"的写作思路。
