# 实验 3.2：从 One-Hot 到 Embedding Matrix

> **实验目标**
>
> 上一节，我们使用 One-Hot Encoding 成功解决了 Token ID 没有语义的问题。
>
> 例如：
>
> ```
> cat
> ```
>
> 可以表示成：
>
> ```
> [0,0,1,0]
> ```
>
> 但是，一个新的问题出现了：
>
> **如果 Vocabulary 有 100000 个 Token，每一个 One-Hot 向量都会有 100000 个维度。**
>
> 本实验将回答：
>
> > **为什么现代 LLM 不使用 One-Hot，而使用 Embedding Matrix？**

---

# 一、One-Hot 的问题

继续使用上一节的 Vocabulary：

| Token | ID |
|------|---:|
| apple | 0 |
| banana | 1 |
| cat | 2 |
| dog | 3 |

One-Hot：

```
apple

↓

[1,0,0,0]
```

```
cat

↓

[0,0,1,0]
```

非常容易理解。

但是：

如果 Vocabulary：

变成：

```
100000 Token
```

那么：

```
cat
```

对应：

```
[0,0,0,0,......,1,......,0]
```

长度：

```
100000
```

其中：

真正有用的数据：

只有：

```
一个 1
```

其它：

```
99999 个 0
```

是不是非常浪费？

---

# 二、再观察一下 One-Hot

假设：

Vocabulary：

```
apple

banana

cat

dog
```

One-Hot：

```
apple

↓

[1,0,0,0]
```

```
banana

↓

[0,1,0,0]
```

```
cat

↓

[0,0,1,0]
```

```
dog

↓

[0,0,0,1]
```

请注意：

真正决定 Token 身份的。

其实只是：

```
哪一个位置是 1
```

因此：

One-Hot 本质上就是：

> **根据 Token ID，选择某一行。**

---

# 三、换一种保存方式

假设。

我们提前准备一个表：

```
Token        Vector

apple

↓

[0.12,0.83]

banana

↓

[-0.55,0.27]

cat

↓

[0.91,-0.18]

dog

↓

[0.88,-0.22]
```

请注意。

现在：

已经不需要：

```
100000 个 0
```

只需要：

找到：

```
cat
```

对应：

```
[0.91,-0.18]
```

即可。

是不是快很多？

---

# 四、这张表叫什么？

这张二维表。

就是：

> **Embedding Matrix**

例如：

```python
embedding = [

    [0.12, 0.83],      # apple

    [-0.55, 0.27],     # banana

    [0.91,-0.18],      # cat

    [0.88,-0.22]       # dog

]
```

这里：

```
4
```

表示：

Vocabulary 大小。

```
2
```

表示：

Embedding Dimension。

真正 GPT：

可能：

```
100000 × 768
```

或者：

```
150000 × 4096
```

原理：

完全一样。

---

# 五、Lookup（查表）

Tokenizer：

输出：

```
cat

↓

2
```

Embedding：

其实什么都没有计算。

它只是：

```
embedding[2]
```

代码：

```python
embedding = [

    [0.12,0.83],

    [-0.55,0.27],

    [0.91,-0.18],

    [0.88,-0.22]

]

token_id = 2

vector = embedding[token_id]

print(vector)
```

输出：

```
[0.91,-0.18]
```

请注意。

这里：

没有：

```
乘法

加法

神经网络
```

只是：

Python：

列表索引。

所以：

Embedding：

又叫：

> **Lookup Table（查找表）**

---

# 六、One-Hot 与 Embedding 的关系

很多教材会说：

Embedding 是 One-Hot 的升级版。

其实：

更准确地说：

> **Embedding Matrix 是 One-Hot 的高效实现。**

为什么？

观察：

One-Hot：

```
cat

↓

[0,0,1,0]
```

如果：

把它：

乘：

Embedding Matrix：

```
            Embedding Matrix

        apple

banana

cat

dog

↓

[1,0]

[2,3]

[4,5]

[6,7]
```

计算：

```
[0,0,1,0]

×

Embedding
```

结果：

是不是：

直接得到：

```
[4,5]
```

也就是：

第三行。

因此：

真正 Transformer。

并不会：

先生成：

One-Hot。

再做矩阵乘法。

它直接：

```
Embedding[token_id]
```

速度：

完全一样。

结果：

完全一样。

只是：

更高效。

---

# 七、完整代码

```python
vocab = [

    "apple",

    "banana",

    "cat",

    "dog"

]

embedding = [

    [0.12,0.83],

    [-0.55,0.27],

    [0.91,-0.18],

    [0.88,-0.22]

]

token = "cat"

token_id = vocab.index(token)

vector = embedding[token_id]

print("Token:", token)

print("Token ID:", token_id)

print("Embedding:", vector)
```

运行：

```
Token: cat

Token ID: 2

Embedding:

[0.91,-0.18]
```

---

# 八、现在这些向量有意义吗？

很多同学看到这里。

会问：

```
[0.91,-0.18]
```

是谁写的？

为什么：

```
cat
```

就是：

```
[0.91,-0.18]
```

答案其实很简单：

> **目前没有任何意义。**

我们只是：

随便写了几个数字。

真正 GPT：

这些数字：

全部都是：

随机初始化。

例如：

```
cat

↓

[0.014

-0.023

0.009

...]
```

然后：

在训练过程中：

不断更新。

越来越合理。

因此：

Embedding：

真正重要的不是：

Lookup。

而是：

> **训练。**

---

# 九、本实验修改了什么？

相比上一节。

唯一变化：

```
One-Hot

↓

Embedding Matrix
```

Language Model：

Attention：

Transformer：

全部没有出现。

只是：

增加了：

```
Lookup
```

整个数据流：

升级成：

```
Text

↓

Tokenizer

↓

Token IDs

↓

Embedding Matrix

↓

Vectors
```

---

# 实验收获（Takeaways）

✅ 理解 One-Hot 为什么效率低。

✅ 理解 Embedding Matrix 的结构。

✅ 理解 Lookup Table 的工作方式。

✅ 理解 Embedding Matrix 本质上是 One-Hot 的优化实现。

❌ 当前 Embedding 向量仍然是随机的，没有语义。

---

# Code Evolution

```
Text
 │
 ▼
Tokenizer
 │
 ▼
Token IDs
 │
 ▼
One-Hot
 │
 ▼
Embedding Matrix
 │
 ▼
Vectors
```

---

# Design Discussion（为什么 Embedding 会有语义？）

请思考：

目前：

```
cat

↓

[0.91,-0.18]
```

完全是：

随机数。

那么：

为什么：

训练结束以后。

会变成：

```
cat

↓

[0.24,0.81,...]
```

并且：

与：

```
dog
```

越来越接近？

是谁修改了：

这些数字？

答案就是下一节：

> **Embedding Training（Embedding 是如何学会语义的）**

下一节，我们将第一次引入**梯度下降（Gradient Descent）**和**反向传播（Backpropagation）**，用一个极简实验观察 Embedding 向量如何一步步学习到语义。这也是整本书第一次真正进入"神经网络训练"阶段。